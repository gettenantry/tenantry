## What & why

<!-- What does this PR change, and what problem does it solve?
     Link the issue: Closes #XX -->

## Checklist

- [ ] Tests added/updated — security-critical paths (context, guard, adapters) target 100% coverage
- [ ] `pnpm lint && pnpm typecheck && pnpm test` pass locally
- [ ] Docs updated (guide pages, API reference, README) if behavior changed
- [ ] Changeset added (`pnpm changeset`) if a published package is affected
- [ ] Commits follow Conventional Commits

## Security note

<!-- Does this change touch tenant isolation in any way (filtering, RLS,
     context propagation)? If yes, explain how the guarantees are preserved
     and which test proves it. -->
