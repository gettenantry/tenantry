generator client {
  provider = "prisma-client-js"
  output   = "./generated/client"
}

datasource db {
  provider = "postgresql"
  url      = env("TENANTRY_TEST_DATABASE_URL")
}

model Project {
  id       String @id
  tenantId String
  name     String
}
