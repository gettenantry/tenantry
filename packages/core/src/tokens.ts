/** Injection token for the options passed to `TenancyModule.forRoot()`. */
export const TENANCY_MODULE_OPTIONS = Symbol('TENANCY_MODULE_OPTIONS');

/** Injection token for the composed `TenantExtractor` built from the options. */
export const TENANT_EXTRACTOR = Symbol('TENANT_EXTRACTOR');
